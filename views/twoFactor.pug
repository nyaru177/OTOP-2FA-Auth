doctype html
html(lang="zh")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title 2FA 验证
    link(rel="stylesheet", href="/stylesheets/auth.css")
    link(rel="stylesheet", href="/stylesheets/alertbox.css")
  body
    .container
      .form-container
        h1 2FA 验证
        p.description 请打开您的 2FA 应用并输入一次性密码
        form(action="/twoFactorVerify", method="POST")
          .form-group
            label(for="otp") 验证码:
            input(type="text", id="otp", name="otp", required)
          button(type="submit") 提交
    script(src="/javascripts/twoFactor.js")